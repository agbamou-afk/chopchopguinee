/**
 * Deprecated: the Radix-based <Toaster /> has been replaced by Sonner
 * (see src/components/ui/sonner.tsx). This stub is kept only so any
 * stale imports keep type-checking; it renders nothing.
 */
export function Toaster() {
  return null;
}
